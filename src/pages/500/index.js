import { Container, Typography } from "@mui/material";

export default function ServerError() {
  return (
    <Container>
      <Typography maxWidth="sm">Server error</Typography>
    </Container>
  );
}